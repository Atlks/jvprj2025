get:
  tags:
    - trans_recharge
  summary: "管理后台 钱包方面接口 拒绝充值"
  description: " 。。。。"

  parameters:
    - name: transactionId
      in: query
      description: "充值单id"
      required: true
      schema:
        type: string

  responses:
    "200":
      $ref: '../../components/responses/CommonResponse.yaml'