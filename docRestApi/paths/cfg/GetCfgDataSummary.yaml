get:
  tags:
    - adm_rpt
  summary: "获取数据总览"
  description: " 。。。。"

  parameters:
    - name: k
      in: query
      description: " "
      required: true
      schema:
        type: string
      example: "DataSummary"


  responses:
    "200":
      $ref: '../../components/responses/CommonResponse.yaml'