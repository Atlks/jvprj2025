get:
  tags:
    - admin
  summary: "add管理员用户的方法"
  description: " 。。。。"

  parameters:
    - name: username
      in: query
      description: "用户名"
      required: true
      schema:
        type: string
    - name: password
      in: query
      description: "密码"
      required: true
      schema:
        type: string

  responses:
    "200":
      $ref: '../../../components/responses/CommonResponse.yaml'