get:
  tags:
    - adm_wlt
  summary: "管理后台 钱包方面接口 拒绝充值"
  description: " 。。。。"
  operationId: "QueryOrdChrgHdr"
  parameters:
    - name: ord_id
      in: query
      description: "订单id"
      required: true
      schema:
        type: string

  responses:
    "200":
      $ref: '../../components/responses/CommonResponse.yaml'